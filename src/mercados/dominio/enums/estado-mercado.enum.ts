/**
 * Enum para el estado de un mercado
 * Define los posibles estados en los que puede estar un mercado
 */
export enum EstadoMercado {
  ACTIVO = 'ACTIVO',
  INACTIVO = 'INACTIVO',
  EN_CONFIGURACION = 'EN_CONFIGURACION',
  SUSPENDIDO = 'SUSPENDIDO',
}